{
  "name": "@agents-for-introverts/context-kernel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Harness-agnostic, local-first context ledger and projection service",
  "bin": {
    "afi-context": "dist/src/cli.js"
  },
  "exports": {
    ".": "./dist/src/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "pretest": "npm --prefix ../../packages/protocol run build",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
